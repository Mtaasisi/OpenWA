/** @deprecated Use useAppNotifications — browser notifications are unified in useAppNotifications */
export function useInboxNotifications() {
  // No-op: useAppNotifications handles web + desktop OS notifications.
}
